[Mount]
What=//{{ item.host }}.{{ ad_domain }}/{{ item.path }}
Where={{ item.mountpoint }}
Options=credentials=/etc/fstab.password,noperm,vers={{ item.version }},_netdev,domain={{ ad_domain.split(".")[0] }}
Type=cifs
