- include_tasks: install.yml
# a zone is pointless without a source defined
- include_tasks: zone.yml
  when: (item.zone is defined) and (item.source is defined)
- include_tasks: service.yml
  when: item.service is defined
